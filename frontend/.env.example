# Port for the local dev server
VITE_APP_DEV_PORT=3000
# Base URL of the backend API
VITE_API_URL=http://127.0.0.1:3001
# Canonical production site origin (no trailing slash) — used to build
# canonical URLs and JSON-LD @id / url values during prerender.
VITE_SITE_ORIGIN=https://ardenroofing.com
